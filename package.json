{
  "name": "github-trending-reports",
  "version": "2.0.0",
  "description": "GitHub Trending 报告生成系统 - 统一架构版本",
  "main": "src/index.js",
  "scripts": {
    "generate:daily": "node scripts/generate-daily.js",
    "generate:weekly": "node scripts/generate-weekly.js",
    "generate:monthly": "node scripts/generate-monthly.js",
    "generate:all": "node scripts/generate-all.js",
    "generate:index": "node scripts/generate-index.js",
    "test": "node --test tests/**/*.test.js",
    "test:loader": "node --test tests/loader/*.test.js",
    "test:analyzer": "node --test tests/analyzer/*.test.js",
    "test:generator": "node --test tests/generator/*.test.js",
    "lint": "eslint src/ scripts/ tests/",
    "format": "prettier --write \"src/**/*.js\" \"scripts/**/*.js\" \"tests/**/*.js\"",
    "verify": "node scripts/verify-structure.js"
  },
  "keywords": [
    "github",
    "trending",
    "ai",
    "report",
    "analysis"
  ],
  "author": "Spock Wen",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/spock-wen/daily_report_analysis.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
